---
import Button from "./Button.astro";
const { title, text, dato } = Astro.props;
---

<article class="card">
    <h3>{title}</h3>
    <p>{dato}</p>
    <p>{text}</p>
    <a href="#0"> <Button variant="primary" size="medium">LÆS MERE</Button></a>
</article>

<style>
  .card {
    display: flex;
flex-direction: column;
align-items: flex-start;
gap:var(--spacing-4-xl);
  }

  p{
   font-size: 0.8rem;
  }

  @media screen and (max-width:899px) {
    .card {
    display: flex;
flex-direction: column;
align-items: center;
gap:var(--spacing-4-xl);
margin: 0 var(--spacing-sm) 0 var(--spacing-sm) ;
  }

  p{
   font-size: 1rem;
  }
    
  }
</style>
